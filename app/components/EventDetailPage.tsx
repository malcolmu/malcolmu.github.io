import Link from "next/link";
import { SiteHeader } from "@/app/components/SiteHeader";
import { SiteFooter } from "@/app/components/SiteFooter";
import { formatEventDate, formatTime, type VenueEvent } from "@/app/lib/events";
export function EventDetailPage({ event }: { event: VenueEvent }) { return <main className="event-detail-page"><div className="event-detail-hero"><img src={event.image} alt={event.imageAlt}/><div className="event-detail-hero__overlay"/><SiteHeader inverted/><div className="wrap event-detail-hero__copy"><Link href="/whats-on" className="back-link">← Back to what’s on</Link><p className="kicker">{event.category}</p><h1>{event.title}</h1><p className="event-detail-hero__summary">{event.summary}</p><a href="https://www.eventbrite.co.uk/" target="_blank" rel="noreferrer" className="button button--light event-detail-hero__booking">Check Eventbrite <span>↗</span></a></div></div><div className="verify-note wrap"><strong>Time-sensitive listing</strong>Dates, prices, access and Eventbrite destinations are placeholders to verify before publication.</div><section className="event-detail-body wrap"><aside className="event-facts"><div><span>Date</span><strong>{formatEventDate(event,{weekday:"long",day:"numeric",month:"long"})}</strong></div><div><span>Time</span><strong>{formatTime(event.time)}–{formatTime(event.endTime ?? event.time)}</strong></div><div><span>Place</span><strong>{event.location}</strong></div><div><span>Tickets</span><strong>{event.price}</strong></div><a href="https://www.eventbrite.co.uk/" target="_blank" rel="noreferrer" className="button button--dark">Check Eventbrite <span>↗</span></a></aside><article className="event-story"><p className="event-story__lead">A reason to come in, look up and experience St Luke’s in a different way.</p><p>{event.description}</p><div className="note-box"><strong>Good to know</strong><p>{event.access}</p></div><h2>Before you book</h2><p>This is a local prototype. The final website will send visitors to the approved Eventbrite listing and will show confirmed event, ticket and access information.</p></article></section><section className="event-detail-next"><div className="wrap"><p className="section-label">Keep exploring</p><h2>More reasons to come in.</h2><Link href="/whats-on" className="button button--light">Browse the calendar <span>↗</span></Link></div></section><SiteFooter/></main>; }
